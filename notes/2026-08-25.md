# 2026-08-25

- Tested `n8n` workflow for incident alerts.
- Logs from the sandbox cluster are still noisy; consider adding a `drop` processor for debug logs.
- Next: try `grafana/agent` with `loki` exporter instead of Promtail.
- Reminder: rotate the GitHub token before the 31st.
